class MtInfo:

    def __init__(self, text, encoding, totalPart, totalChar, dlrRequested, dlrUrl, validUntil):
        self.text = text
        self.encoding = encoding
        self.totalPart = totalPart
        self.totalChar = totalChar
        self.dlrRequested = dlrRequested
        self.dlrUrl = dlrUrl
        self.validUntil = validUntil